# Taking a string input from the user
user_input = input("Enter a list of items separated by spaces: ")

# Converting the string to a list (splitting by space)
items = user_input.split()

# Printing the list
print(items)
